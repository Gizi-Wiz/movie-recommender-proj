const Header = () => {
    return(
        <header className="header">
            <div className="header-left">
                <span clasName="header-brand">MyReact App</span>
            </div>
            <div className="header-right">
                <a href="#">HOME</a>
                <a href="#">Contact</a>
                <a href="#">About us</a>
            </div>
        </header>
    )
}

export default Header;